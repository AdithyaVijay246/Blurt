[package]
name = "blurt-router"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
thiserror = "2.0.20"
uuid = "1.24.1"

# Routing decisions are made against the user's real `destinations` table, so
# this crate reads through Module 2's repository layer. It never writes — see
# the crate docs for why the write stays in blurt-app.
blurt-schema = { workspace = true }
rusqlite = "0.40.2"
